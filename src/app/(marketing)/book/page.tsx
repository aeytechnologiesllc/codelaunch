import { ClientIntakeForm } from "@/components/intake/ClientIntakeForm";

export default function BookPage() {
  return (
    <ClientIntakeForm
      eyebrow="Portal Access"
      title="Continue in the Portal"
      description="If you were expecting a booking page, this is the better version. Tell us what you need and we will continue in your portal workspace instead of scheduling a call."
    />
  );
}
