import { ClientIntakeForm } from "@/components/intake/ClientIntakeForm";

export default function ContactPage() {
  return (
    <ClientIntakeForm
      eyebrow="Portal Intake"
      title="Start Your Project"
      description="Send your project details once, then keep everything organized in the portal. No phone calls, no back-and-forth across random channels."
    />
  );
}
